package bootstrapper

/*import "golang.design/x/hotkey"

func InitalizeHotkeys() {
	hotkeyFunctions := map[string]func(testRegistration bool) bool{
		Config.PTTHotkey: func(testRegistration bool) bool {
			if testRegistration {
				return Config.PTTEnabled
			}
			return true
		},
	}

	for hk, event := range hotkeyFunctions {
		hotkey.New()
	}
}
*/
